# Secure Authentication Settings
ADMIN_EMAIL=admin@example.com
# Use node -e "const crypto = require('crypto'); const salt = crypto.randomBytes(16).toString('hex'); const hash = crypto.scryptSync('yourpassword', salt, 64).toString('hex'); console.log(salt + ':' + hash);"
ADMIN_PASSWORD_HASH=
SESSION_SECRET=
